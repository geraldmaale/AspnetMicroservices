@inject NavigationManager NavigationManager

@code {

    protected override void OnInitialized()
    {
        //NavigationManager.NavigateTo($"https://localhost:5001/account/login?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}");
        NavigationManager.NavigateTo($"/loginidp?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", true);
    }

}